# Application Configuration
spring.application.name=cloud-security-platform
server.port=8080

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:5173

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=mysecretpassword
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true

# Logging
logging.level.root=INFO
logging.level.com.cloudsec=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n

# Actuator & Prometheus
management.endpoints.web.exposure.include=health,prometheus,metrics
management.endpoint.health.show-details=always
management.metrics.export.prometheus.enabled=true

# Scheduling
spring.task.scheduling.pool.size=5

# Scheduler Configuration (in milliseconds)
scheduler.detection.enabled=true
scheduler.detection.interval=300000
scheduler.remediation.enabled=true
scheduler.remediation.interval=120000
scheduler.remediation.auto-remediate-high=true